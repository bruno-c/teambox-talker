<div class="sheet">
  <h2><%= @title = "New notification" %></h2>

  <% form_for @notification, :html => { :class => "lw120" } do |f| %>
    <%= f.error_messages %>
    
    <%= render f %>
    
    <p class="indent submit">
      <%= buttons "Create", :cancel_to => notifications_path %>
    </p>
  <% end %>
</div>