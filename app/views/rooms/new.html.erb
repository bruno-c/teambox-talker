<div class="sheet">
  <h2><%= @title = "New room" %></h2>

  <% form_for [current_account, @room], :html => { :class => "lw120" } do |f| %>
    <%= render f %>
    <p class="indent submit">
      <%= buttons "Create", :cancel_to => account_rooms_path(current_account) %>
    </p>
  <% end %>
</div>
