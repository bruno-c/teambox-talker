<div id="nav">
  <ul class="specific">
    <% if logged_in? %>
      <li class="user"><%= link_to "Logged in as #{current_user.name}", settings_path, :title => "Logged in as #{current_user.name}" %>.</li>
      <li><%= link_to "Rooms", rooms_path, :title => "Rooms" %></li>
      <% if admin? %>
        <li><%= link_to 'Users', users_path %></li>
        <li><%= render :partial => "logs/search_form" %></li>
      <% end %>
    <% end %>
  </ul>
<!--  <p class="utilities">
    <ul>
      <% if logged_in? %>
        <li>Logged in as
        <%= link_to image_tag("avatar_default.png", :alt => current_user.name), settings_path, :title => "Logged in as #{current_user.name}, manage my account", :class => "avatar" %></li>
        <li><%= link_to current_user.name, settings_path, :title => "Logged in as #{current_user.name}" %>.</li>
        <li><%= link_to "Logout", "", :title => "Logout" %></li>
      <% else %>
        <li><%= link_to "Login", login_path, :title => "Login" %></li>
        <li><%= link_to "Sign Up", signup_path, :title => "Sign Up" %></li>
      <% end %>
    </ul>
  </p>-->
</div>